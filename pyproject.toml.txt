[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "witness"
version = "1.0.0"
description = "Deterministic AI decision witness"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[project.scripts]
witness = "witness.cli:main"
